I want an in-app editor for customizing agent prompts. The agents are .md files in custom-agents/ and the README says they're fully editable, but right now I have to open them in an external text editor. That's friction that most users won't push through.

A dedicated section in settings (or a tab on each agent's header in the chat view) that shows the agent's system prompt in a nice Markdown editor, with a "Reset to default" button that restores the bundled version. Should show the filename and last-modified timestamp. Verity is special — she has multiple sub-prompts (CORE, SCAFFOLD, DRAFT, REVISION, MECHANICAL, LEDGER, AUDIT, FIX) so the editor should show all of them as tabs.

This is the single most powerful customization hook in the app and it's hidden behind "find the userData directory and edit a file." Surface it.
