I want a dedicated view that shows me real data about my writing. Word count over time (per day, per week), words per chapter, tokens spent per agent, cost per pipeline phase, and a rough $/chapter estimate. The data is already in SQLite — token_usage, file_versions timestamps, word counts — it just needs to be surfaced.

A chart showing manuscript word count growth over time would be the killer feature. Show me when I was productive and when I stalled. Let me filter by book or see all books. Include a "cost breakdown" section that shows how much each agent has consumed in tokens across the project, and a per-phase breakdown so I can see if my revision cycle is more expensive than drafting.

This should be its own view accessible from the sidebar, not buried in settings. The current usage section in settings shows aggregate totals — that's a start but I want the story of the project, not just the summary.
